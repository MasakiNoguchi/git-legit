<%= simple_form_for [@project, @contribution] do |f| %>
<%= f.input :title %>
<%= f.input :description %>
<%= f.input :guidelines %>
<%= f.input :status, collection: ["open", "booked", "closed"] %>
<%= f.association :user, collection: User.all , label_method: :email %>
<%= f.submit %>
<% end %>
